# Project Conventions

- Stop clearly when a required tool is unavailable.
- Keep project secrets in gitignored local files.
- Prefer deterministic project scripts over manual repeated work.
- Use adapters only when no stable API, MCP server, or project script exists.
