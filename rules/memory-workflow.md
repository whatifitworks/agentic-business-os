# Memory Workflow

Generated artifacts with durable value start in `inbox/`. Use `memory-ingest` to promote concise synthesis to `wiki/`, preserve sources, move final deliverables to `outputs/`, record process-only outcomes, or drop low-signal items.

Do not write raw transcripts, secrets, private customer content, or bulky command output into durable memory.
