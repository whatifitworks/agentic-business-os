# Adapter Sources

Adapter source contracts and run evidence live here.

Use these files to preserve evidence quality for UI-driven workflows. A successful adapter run should include the workflow name, target, timestamp, inputs, extracted values or artifact paths, evidence path, confidence, and caveats.
